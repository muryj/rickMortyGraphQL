import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    height: 60,
    borderWidth: 3,
    borderColor: 'yellow',
    justifyContent: 'center',
    alignItems: 'center'
  },
  buttonText: {
    color: 'yellow',
    fontSize: 20,
  }
});
