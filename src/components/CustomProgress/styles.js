import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  image: {
    height: 200,
    width: 200,
    resizeMode: 'contain',
    position: 'absolute',
    top: 100,
    left: 100
  },
});
